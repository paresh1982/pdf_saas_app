{
  "name": "onestopdoc",
  "version": "3.0.0",
  "private": true,
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "start": "node backend/server.js",
    "build": "npm run build -w frontend && python3 -m pip install -t backend/python_libs -r backend/requirements.txt",
    "dev": "npm run dev -w backend"
  },
  "dependencies": {
    "@vitejs/plugin-react": "^4.3.1",
    "vite": "^5.4.2"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
