{
  "name": "onestopdoc",
  "version": "3.0.0",
  "private": true,
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "start": "node backend/server.js",
    "build": "npm run build -w frontend",
    "dev": "npm run dev -w backend"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
